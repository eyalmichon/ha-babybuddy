#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

echo "Installing Python requirements..."
sudo uv pip install --system ".[dev]"

echo "Installing frontend dependencies..."
cd frontend_src && npm install && cd ..

echo "Setup complete!"
